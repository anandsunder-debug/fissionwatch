// Inert external solid mock-up; dimensions in millimeters
// Assumed dimensions, not video-verified. No internal or propulsion features.
$fn = 96;
rotate([0,90,0]) cylinder(h=100.0, r=1.5, center=false);
